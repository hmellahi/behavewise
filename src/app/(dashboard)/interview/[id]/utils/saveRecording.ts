import { convertRecordingToVideo } from "./convertRecordingToVideo";

const saveRecording = (recordedChunks: Blob[]) => {
  // const video = convertRecordingToVideo(recordedChunks);
  // Cloudinary.saveVideo(recordedChunks);
  return null;
};

export default saveRecording;
